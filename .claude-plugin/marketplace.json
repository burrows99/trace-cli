{
  "name": "trace-oss",
  "description": "Unified execution tracer (Node/Chrome) — the `trace` CLI + usage skill.",
  "owner": {
    "name": "Raunak Burrows"
  },
  "plugins": [
    {
      "name": "trace",
      "source": ".",
      "description": "Set breakpoints + fire a trigger → a full execution trace as one JSON envelope. Node/Chrome over CDP; plus `trace serve` for a realtime trace UI. Adds the `trace` binary to PATH."
    }
  ]
}
