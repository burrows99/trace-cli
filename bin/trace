#!/usr/bin/env node
// trace — CLI entry point. Thin launcher; the class-first application lives in src/ (TypeScript) and is
// compiled to dist/. `dist/cli/main.js` imports reflect-metadata first, then runs the Cli.
import "../dist/cli/main.js";
