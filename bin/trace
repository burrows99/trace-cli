#!/usr/bin/env node
// trace — CLI entry point. Thin launcher; logic lives in src/cli.js so the same code is reachable as a
// library too. Referenced by package.json "bin" and placed on PATH by the Claude Code plugin.
import { run } from "../src/cli.js";
run();
